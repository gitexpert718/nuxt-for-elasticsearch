{
  "name": "testapp",
  "version": "1.0.0",
  "description": "rendering",
  "author": "jin",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@appbaseio/reactivesearch": "^3.8.3",
    "@appbaseio/reactivesearch-vue": "^1.3.0",
    "ant-design-vue": "^1.1.10",
    "cross-env": "^5.2.0",
    "express": "^4.16.4",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.9"
  }
}
